# 全新部署请先运行 ./start.sh；脚本会生成第二个 include 所需的 Supabase 配置。
include:
  - path: ./docker-compose.app.yml
  - path: ${FAMILY_APP_SUPABASE_COMPOSE_PATH:-./.runtime/local-supabase/docker/docker-compose.yml}
    env_file: ${FAMILY_APP_SUPABASE_ENV_PATH:-./.runtime/local-supabase/docker/.env}
