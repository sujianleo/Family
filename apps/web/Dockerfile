ARG NODE_IMAGE=node:22-alpine

FROM ${NODE_IMAGE} AS dependencies
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci

FROM ${NODE_IMAGE} AS builder
WORKDIR /app
ARG NEXT_PUBLIC_FAMILY_APP_LAN_ADDRESS=""
ARG NEXT_PUBLIC_FAMILY_APP_AUTH_REQUIRED="false"
ARG NEXT_PUBLIC_FAMILY_APP_AUTH_PROVIDER=""
ARG NEXT_PUBLIC_SUPABASE_URL=""
ARG NEXT_PUBLIC_SUPABASE_PUBLIC_URL=""
ARG NEXT_PUBLIC_SUPABASE_ANON_KEY=""
ARG NEXT_PUBLIC_SUPABASE_LAN_PORT="8000"
ARG NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
ENV NEXT_TELEMETRY_DISABLED=1
ENV NEXT_PUBLIC_FAMILY_APP_LAN_ADDRESS=$NEXT_PUBLIC_FAMILY_APP_LAN_ADDRESS
ENV NEXT_PUBLIC_FAMILY_APP_AUTH_REQUIRED=$NEXT_PUBLIC_FAMILY_APP_AUTH_REQUIRED
ENV NEXT_PUBLIC_FAMILY_APP_AUTH_PROVIDER=$NEXT_PUBLIC_FAMILY_APP_AUTH_PROVIDER
ENV NEXT_PUBLIC_SUPABASE_URL=$NEXT_PUBLIC_SUPABASE_URL
ENV NEXT_PUBLIC_SUPABASE_PUBLIC_URL=$NEXT_PUBLIC_SUPABASE_PUBLIC_URL
ENV NEXT_PUBLIC_SUPABASE_ANON_KEY=$NEXT_PUBLIC_SUPABASE_ANON_KEY
ENV NEXT_PUBLIC_SUPABASE_LAN_PORT=$NEXT_PUBLIC_SUPABASE_LAN_PORT
ENV NEXT_PUBLIC_VAPID_PUBLIC_KEY=$NEXT_PUBLIC_VAPID_PUBLIC_KEY
COPY --from=dependencies /app/node_modules ./node_modules
COPY . .
RUN npm run build

FROM ${NODE_IMAGE} AS runner
WORKDIR /app
ENV NODE_ENV=production \
    NEXT_TELEMETRY_DISABLED=1 \
    HOSTNAME=0.0.0.0 \
    PORT=3000

RUN addgroup --system --gid 1001 nodejs \
    && adduser --system --uid 1001 nextjs \
    && mkdir -p /app/data \
    && chown -R nextjs:nodejs /app/data

COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static
COPY --from=builder --chown=nextjs:nodejs /app/public ./public

USER nextjs
EXPOSE 3000
CMD ["node", "server.js"]
